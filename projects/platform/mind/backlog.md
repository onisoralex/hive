# Backlog — Platform

Items discovered during work that aren't immediately actioned.

Format: `- [ ] <item> _(discovered during: <module/phase>)_`

---

- [ ] Implement `@hive/auth` Clerk integration for Next.js _(implement when first product project needs auth)_
- [ ] Implement `@hive/billing` Stripe credit pack checkout _(implement when first product project needs payments)_
- [ ] Implement `@hive/analytics` PostHog setup _(implement when first product project deploys)_
- [ ] Implement `@hive/ads` AdMob banner component for React Native _(implement when first Android app is ready)_
- [ ] Implement `@hive/ui` base component set (Button, Input, Card, Dialog) _(implement when first web product needs shared components)_
- [ ] Set up root `package.json` with npm workspaces _(implement when first product consumes a platform package)_
