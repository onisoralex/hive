# coffee-shop — Hive context

Read the project repo's `AGENTS.md` for app context (path from `project.md`).

## Spawning notes
- This project uses `AGENTS.md` (not `CLAUDE.md`) as its app context file — it has been handed off to an organisation and uses the agent-agnostic format.
