# <project-name> — Hive context

Read the project repo's `CLAUDE.md` for app context (path from `project.md`).

## Spawning notes
<!-- Add project-specific worker instructions here, e.g.:
- Developer: always include the react-native extension (Expo project)
-->
